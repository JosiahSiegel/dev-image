/**
 * Defines the configuration for the development container, including the name, build settings, and command override.
 */
{
    "name": "Developer CLI",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "overrideCommand": false,
    "runArgs": ["--env-file",".devcontainer/devcontainer.env"]
}
